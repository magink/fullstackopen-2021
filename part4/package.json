{
  "name": "bloglist",
  "version": "0.0.1",
  "description": "Allows users to save information about interesting blogs they find on the web",
  "main": "index.js",
  "author": "magink",
  "license": "MIT",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "lint": "eslint .",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "lodash": "^4.17.21",
    "mongoose": "^5.12.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "eslint": "^7.23.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7"
  }
}
